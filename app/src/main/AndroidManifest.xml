<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exercisee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Exercisee">
        <activity android:name=".Pendaftaran" android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".ActLihatData" android:parentActivityName=".Home_Act" android:label="@string/app_name"/>
        <activity android:name=".Home_Act" android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".validasi_ep"
            android:label="@string/label2"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>